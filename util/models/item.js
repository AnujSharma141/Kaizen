import jsdom from "jsdom"
const {JSDOM} = jsdom

class Item {
    data = new dom(src)

    constructor(data){
        this.name = data.document
        this.link = ''
        this.rating = '' 
        this.image = this.img(data)
    }

    feed(src){
        const data = new dom(src)
    }

    img(data){
        return this.img
    }
}